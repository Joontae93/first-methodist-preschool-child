export default function getLunches() {
	const button = document.getElementById('lunch');
	const container = document.getElementById('lunchContainer');
	button.addEventListener('click', (ev) => {
		ev.preventDefault();
		container.innerHTML = '';
		// 3. Get Lunches

		// 4. Display Lunches
	});
}
